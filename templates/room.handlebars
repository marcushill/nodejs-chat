<div class="tab-pane active full-height" id="{{target}}-content12">
    <div class="col-xs-12 col-md-10 full-height">
        <div class="row main-chat">
            <div class="col-xs-12">
                <ul id="{{target}}-messages" class="list-group messages">
                </ul>
            </div>
        </div>
        <div class="row input-row">
            <div class="col-xs-12">
                <form action="" id="{{target}}-send" class="send-form">
                    <div class="form-group well well-sm anchor-bottom">
                        <div class="input-group">
                            <input id="{{target}}-message" autocomplete="off" class="form-control sender" />
                            <span class="input-group-btn"><button class="btn btn-danger">Send</button></span>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
    <div class="col-md-2 full-height">
        <div class="well full-height">
            <ul id="{{target}}-users" class="list-group user-list">
            </ul>
        </div>
    </div>
</div>